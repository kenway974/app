<form method="post" action="{{ path('app_trophy_delete', {'id': trophy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trophy.id) }}">
    <button class="btn">Delete</button>
</form>
