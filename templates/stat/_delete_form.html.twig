<form method="post" action="{{ path('app_stat_delete', {'id': stat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stat.id) }}">
    <button class="btn">Delete</button>
</form>
